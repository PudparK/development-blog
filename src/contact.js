console.log("Hello from contact.js");

const css = require('./app.sass');

import React from 'react';
import ReactDOM from 'react-dom';

var hello = <h1>Hello, from Contact.js!</h1>;

ReactDOM.render(
  hello,
  document.getElementById('root')
);